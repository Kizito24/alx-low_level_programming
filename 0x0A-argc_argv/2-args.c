#include <stdio.h>

/**
 * main - A program that prints all its args
 * @argc: Argument count
 * @argv: Argument Vector
 *
 * Return: Always 0
 */

int main(int argc,  char *argv[])
{
	int n;
	
	for(i = 0; n < argc; n++)
	{
		printf("%s\n", argv[n]);
	}
	return (0);
}
