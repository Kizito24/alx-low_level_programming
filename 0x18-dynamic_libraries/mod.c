#include <stdio.h>

/**
 * mod - find remainder of a number after division
 * a@: number to be divided
 * b@: number to divide
 *
 * Return: res
 */

int mod(int a, int b)
{
        int res;

        res = a % b;

        return res;
}  
